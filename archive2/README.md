# english2-daily

> 英语二的每日一句，翻译 → 自动批改 → 拆解 → Q&A探索。

一个人的考研英语二备考工具——句子驱动，把"写译"接上"自动批改"和"逐句拆解"，专治"看着认识、译出来对不上"。**四战**冲刺，目标从 **39 分**跑到 **65+**，考试日 2026-12-18。

## 架构

```
studio.sh ──▶ studio.py (Python, stdlib only, 127.0.0.1:8787)
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
     src/         fool/       probe/
   原句+批改      逐句拆解    今日诊断
```

`auto-review.sh` 守护进程跑在 Dell，检测到新翻译后唤醒一次 Cursor Agent，顺序完成批改 → fool → probe 三步，三步全部验证通过后一次性 push。单文件前端 `scripts/studio.html`，和纸风格三栏布局，无框架，仅标准库，git 同步跨机器。

## 功能

### 今日写译
- **贴原句 → 写翻译 → 保存**：studio 页面完成全部输入，auto-review ≤15s 接手批改
- **批改格式**：逐点批改 + 参考译文 + 评分表（每个采分点标 ✓ △ ✗）+ Vocab / Phrases 各自带词源和 3 条例句
- **首译锁定**：批改完成后首译不可再改，防止改完答案刷分；参考译文藏在 HTML 注释里，渲染不可见

### 逐句拆解（Fool）
- **批改里出现的所有英文句子**——原句、Vocab 例句、Phrases 例句——全部过四步：扫词 → 扫词块 → 扫句式 → 读句子
- **Vocab**：每个单词词源 + 3 条同难度例句，每条例句本身也走四步；短语同标准
- 目的是把批改里更难的例句也消化掉，不只是看完就过

### 今日诊断（Probe）
五段定式，每天固定输出：
- **今日刺痛**：这次译文直接失分的地方
- **内功印证**：从词根/语义层面解释为什么会错
- **招式印证**：遇到同类结构该怎么处理
- **提分台阶**：直接可用的具体提分项
- **今日带走**：一句话总结

### 复习区
- **每句一生两遇**：第 3 次前和第 7 次前自动抽取，不带旧答案写入新一天的复习区
- **盲译重做**：不看旧答案直接重译，批改对比首译得分（↑ → ↓）
- **历史天可补译**：翻阅任意历史天，复习块未批改时依然可写译并触发批改，不绑定当天

### 单词台
- **一屏 ≤8 词**：词 + 2–3 条例句一次端上来，中文用胶带遮住，口译后点揭开对一眼（`bash word.sh`，端口 8788）
- **只读不评**：没有"认识/生"按钮，不自评，只管反复见；按渐宽间隔调度（1/2/4/7/12/20/30/45/60 天），永不毕业

### 坚持追踪
- **热力图**：过去约一年每天是否练习，颜色深浅按首译分数（5 档），今天红框标出
- **评分走势表**：最近 10 天首译 + 重译评分，↑ ↓ 趋势一眼可见

### 跨机器同步

| 场景 | 模式 | 保存后做什么 |
|------|------|-------------|
| Dell（跑着 auto-review） | 本地模式 | 写 markdown → touch 唤醒，daemon ≤15s 接手 |
| 其他笔记本 | 远程模式 | 写 markdown → commit + push，Dell pull 后处理；页面每 60s 自动 pull |

## 使用

```bash
bash studio.sh      # 主入口：写译 / 重译 / 看批改 / 走势图（端口 8787）
bash word.sh        # 单词复习页：一屏 ≤8 词连例句（端口 8788）
```

不在电脑前时，GitHub 上的轻量入口是 `console/today.md`——倒计时 + 今日评分 + fool/probe 链接，← → 历史导航。

## Daemon

```bash
nohup ./auto-review.sh >> .auto-review.log 2>&1 &
tail -f .auto-review.log   # 查看进度
pkill -f auto-review.sh    # 停止
```

Dell WSL2 安装 Cursor Agent：`curl https://cursor.com/install -fsS | bash`，`agent login` 或设 `CURSOR_API_KEY`。

本地诊断单日完成度：

```bash
bash scripts/verify-day.sh src/june/0622-day60.md
```

## 文件

| 目录 / 文件 | 内容 | 说明 |
|-------------|------|------|
| `src/` | 原始句子文件 | 批改结果写回这里；格式规范见 `src/standard.md` |
| `fool/` | 逐句四步拆解 | 覆盖批改中出现的所有英文句（原句 + Vocab + Phrases 例句）；规范见 `fool/standard.md` |
| `probe/` | 今日诊断 | 刺痛 / 内功印证 / 招式印证 / 提分台阶 / 今日带走；规范见 `probe/standard.md` |
| `console/` | 控制台 | `today.md` 倒计时 + 今日链接；`scores.md` 全部评分 |
| `scripts/` | 工具脚本 | studio.py / review.py / scores.py / word.py，详见 `scripts/README.md` |

## 进度

65 句入库（截止 day65），fool + probe 双轨同步，复习区盲译 → 批改全流程跑通。分数汇总见 `console/scores.md` 和 studio 走势图。

## 路线图

### 已完成
- [x] 写译 → 自动批改（本地 daemon + 远程 git push 双模式）
- [x] Vocab / Phrases 各 3 例句 + 词源，例句本身也过四步
- [x] Fool 逐句四步拆解（覆盖批改中所有英文句）
- [x] Probe 今日诊断五段定式
- [x] 复习区（每句两遇，盲译 + 批改对比）
- [x] 历史天复习块可补译，不绑定当天
- [x] 坚持热力图 + 评分走势表
- [x] 单词台（胶带遮盖，渐宽间隔调度）
- [x] 跨机同步，studio 行为自动适配，零配置
- [x] 每日 console 快照（GitHub 轻量入口）

### 下一步
- [ ] probe 笔记时间线（批改暴露的句法疑问持久化）
- [ ] 单词台记忆闭环完善
- [ ] 大题（翻译/作文）训练模块
