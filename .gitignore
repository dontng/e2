# runtime lock & flag files
.auto-review.lock
.auto-review-processing
.auto-review-push-pending

# python bytecode
scripts/__pycache__/
.auto-review-wake

# 单词复习本地状态（每机一份，不同步）
data/vocab.json

# 深色模式偏好（每机一份，不同步）
data/theme
