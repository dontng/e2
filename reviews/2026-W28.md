# 2026-W28 Review

范围：2026-07-06 至 2026-07-11。当前包含 day72 到 day77；后续若新增本周日文件，继续追加到本文件。

## 本周句子

- `0706-day72`：反对基因专利的三条理由：自然产物不得专利化、基因专利压制创新、专利垄断限制基因检测可及性。
- `0707-day73`：经济衰退期毕业并非同等伤害所有人，精英大学毕业生较快追上本来轨道，而其下大多数人被落下。
- `0708-day74`：发展中国家担心移民时，通常担忧最优秀人才流向硅谷以及发达国家的医院和大学。
- `0709-day75`：Priestly 解释助理毛衣的深蓝色如何从时装秀下沉到百货商店，最后进入廉价货筐。
- `0710-day76`：美国联邦贸易委员会提议在互联网浏览器中增加“请勿追踪”选项，让用户向广告商表达不希望被跟踪。
- `0711-day77`：过去包含通向未来的关键；已识别出的长期模式能支持我们预测后代将身处的处境。

## T1 Error Board

| 项目 | 内容 |
|---|---|
| Top 3 Error Patterns | 固定表达识别；长句主干锁定；专名/技术载体识别 |
| Lowered / Killed | 暂无，当前样本仍在暴露高频风险 |
| Still Alive | 介词短语合读、回指省略、分词修饰、核心动词落地 |
| Next Trap | 简单词固定短语、长专名机构、`in which / find oneself in` 处境结构 |
| Next Preflight | 简单词连用先试短语；长句先锁主干；大写长串先判机构/专名 |

## 高频能力问题

- **学术动词义项**：0707 的 `suggests that` 被译成“建议道”，说明研究/数据作主语时的“表明”义项还需要固定。
- **核心动词落地**：0706 的 `suppress` 和 0709 的 `descended` 都出现核心动作没有译出的问题；不会时要靠上下文路径或对照关系逼方向。
- **介词短语合读**：0706 的 `access to` 被误成“进行”，0707 的 `graduating into a recession` 被误成“毕业后的时期”，0708 的 `at the prospect of` 也没有合读成“对……前景担忧”。
- **固定表达识别**：0708 的 `best and brightest` 被逐词译成“最好最明亮”，0709 的 `bargain bin` 被误成二手店，说明简单词组合成短语后仍要整体判断。
- **省略与指代还原**：0706 的 `Myriad’s` 没有补出 `genetic tests`；0709 的 `in which` 需要回指 `bargain bin`。
- **虚拟与强调结构**：0707 的 `otherwise would have been if ...` 没还原成“本来会处在的位置”，末尾 `it is ... that ...` 结论漏译。
- **专名辨认**：0709 的 `Priestly` 被误读成“最先”，说明句首大写词要先按专有名词试读。
- **技术与机构名识别**：0710 的 `America's Federal Trade Commission` 被译成会议，`internet browsers` 被译成网页，说明长专名和技术载体需要先按现实类别定位。
- **简单词固定表达**：0711 的 `hold the key to`、`find themselves` 都按字面拼错；这与 0708 `best and brightest`、0709 `bargain bin` 属于同一类高频风险。
- **长句主干与分词修饰**：0711 没抓住 `we have identified ... to make forecasts`，也没读出 `patterns shaping the history...`，导致整句坍塌成散词组合。

## 批改质量审计

- `0706-day72`：批改抓住 `suppress`、`reward it`、`access to` 和 `Myriad’s` 四个主要扣分点，Vocab/Phrases 与错误密度匹配。可继续观察法律语气 `may not be patented` 是否需要在后续同类句中反复强化。
- `0707-day73`：批改覆盖主干、研究动词、经济衰退期毕业、虚拟参照点和末尾强调句。该句错误密度高，Phrases 数量偏多但合理，因为主要问题集中在整块结构合读。
- `0708-day74`：批改集中处理 `concerned at the prospect of`、`best and brightest`、`departure to ...`、`developed world` 的层级关系。Vocab 收 `migration/prospect/departure/bright`，短语区覆盖主要合读失败点。
- `0709-day75`：批改抓住专名误读、`descend from A to B to C` 路径、`bargain bin` 和 `in which` 回指。词汇和短语较多，但该句错误点主要集中在词义、路径和回指，覆盖密度合理。
- `0710-day76`：批改覆盖 FTC 机构名、`proposed adding ... to browsers` 和 `so that` 目的关系。Vocab/Phrases 聚焦 `commission`、`browser`、`do not track`，与真实误译匹配。
- `0711-day77`：批改抓住 `hold the key to`、`identified enough of patterns`、`patterns shaping...`、`make forecasts` 和 `find themselves`。该句错误密度高，批改选择 3 个大块处理，避免碎片化。

## 下周观察

- 研究、数据、证据作主语时，先把 `suggest / indicate / show` 归入“表明”通道。
- 核心动词不会时，不要留英文；先看并列、转折、路径、对照结构，推出褒贬和动作方向。
- 遇到 `access to`、`graduate into`、`at the prospect of`、`descend from A to B` 这类介词短语，先整体读，再决定中文动词。
- 看到所有格省略、关系代词和代词时，强制回前文补名词；不要让公司名、人名、地点名词悬空。
- 长句最后一段常是结论，尤其是冒号、分号后的强调句，不能因为前面难就放弃尾句。
- 句首大写词先按专有名词处理，再考虑普通副词或形近词。
- 长句里出现 `have done ... to do ...` 时，先画出“已经做了什么，从而能做什么”的主干关系。
- `find oneself in + situation/position/world` 先译成“身处某种处境”，不要按“寻找自己”处理。

## 当前待办

- 若新增 0712 及之后内容，继续放入本周审计范围或按新周入口拆分。
- 后续可把 0708–0711 的错误归入“简单词固定表达 + 长句主干锁定”专项，作为英二翻译固定训练块。
