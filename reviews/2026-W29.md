# 2026-W29 Review

范围：2026-07-13 至 2026-07-18。当前包含 day78 到 day83；后续新增本周文件时继续追加。

## 本周句子

- `0713-day78`：只有联邦政府有权制定统一归化规则，且联邦法律优先于州法律；这两项宪法原则没有争议。
- `0714-day79`：多数人会陷入精神消沉并把它归因于心理状况，直到置身花园时感觉压抑仿佛被魔法驱散。
- `0715-day80`：一则来自棉花产区的笑话讽刺现代纺织厂自动化程度之高：人喂狗，狗不让人靠近机器。
- `0716-day81`：高失业率和中产收入下降既源于大衰退后的需求下滑，也源于全球化和信息技术更快地用机器或外国劳工替代本地劳动力。
- `0717-day82`：在平均水准已不够用的世界里，最重要的就业措施是通过面向21世纪的教育保障法案，让每个美国人都有机会接受高中后教育。
- `0718-day83`：与希望在美国永久安家的人一同到来的，还有无意留下、只想挣钱后回乡的人。

## T1 Error Board

| 项目 | 内容 |
|---|---|
| Top 3 Error Patterns | 整句判断与真主语定位；固定表达整体识别；从句挂载与论元方向 |
| Lowered / Killed | 暂无；Day 78、79 均出现旧风险复发 |
| Still Alive | `that/which` 从句边界、回指补全、否定比较关系、倒装后的真主语、词义正反方向 |
| Next Trap | `nothing/no one + more ... than` 的最高级含义；句首伴随块后的倒装；连续关系词的挂载归属 |
| Next Preflight | 否定比较级先还原完整关系；前置块后找动词与真主语；每个关系词先回填头名词 |

## 理解出口审计

这张表不重复记录错词；它记录每次阅读最先卡住的位置。后续同一标签在不同题面中再次出现，说明是理解方式问题，不是单日记忆问题。

| 样本 | 主标签 | 次标签 | 当时缺少的出口 |
|---|---|---|---|
| 0713-day78 | `出口/期待` | `出口/义项`、`出口/挂载` | 看到抽象名词与两个 `that` 后提前封口，没有保留“principles 最后怎么样”的等待位 |
| 0714-day79 | `出口/义项` | `出口/关系`、`出口/语境` | `give` 只激活“给”；`blame A on B` 没先说通“把 A 算到 B 头上” |
| 0715-day80 | `出口/义项` | `出口/关系` | `make it`、`relate` 没有从常见义走到句中义；`keep A away from B` 没保住持续状态与对象关系 |
| 0716-day81 | `出口/期待` | `出口/关系`、`出口/重组` | 双因果骨架未保留，`replace A with B` 中被替代者和替代者的位置随后倒置 |
| 0717-day82 | `出口/关系` | `出口/挂载`、`出口/语境` | 没有把“没有什么比 X 更重要”还原成最高级判断，两个 `that` 的归属也随之散开 |
| 0718-day83 | `出口/重组` | `出口/义项`、`出口/关系` | 未把 `Along with A came B` 恢复成 `B came along with A`，因而把两类人的对照压成了同一组人 |

## 高频能力问题

- **整句主干封口**：0713 没有找到 `The Constitutional principles ... are noncontroversial`，把两个 `that` 从句内部当成全句主体，末尾真正结论被留成英文。这和 0711 长句主干断裂同属高风险，但本次更具体：长名词主语后插入多个从句时，必须越过修饰寻找谓语。
- **并列从句边界**：0713 的 `that A and that B` 是两个同级内容块，共同说明 `principles`。若不先标并列层级，就会把第二项原则误接成第一项内部动作。
- **制度语境定向**：0713 的 `Washington` 代指联邦政府，`precede` 表示法律效力优先。词不熟时仍应依据“联邦法—州法”的层级关系限制猜测方向，不能译成没有宾语逻辑的“执行州法律”。
- **动态 probe：期待位缺失**：用户讨论中指出，批改给出“两项宪法原则没有争议”仍然被动，因为原句触发点没有被自己看见。更精确的问题不是泛泛“长句主干”，而是读到 `The Constitutional principles that...` 时提前封口，没有保留“这个抽象名词最后怎么样”的槽位。
- **动态 probe：头名词挂载链过载**：用户进一步指出，多个 `that / which / -ing / -ed / 介词短语 / 逗号补充` 连续服务同一个头名词时，即便信息是直球给出，也会因不知道归属和中文顺序而崩溃。该问题应单独跟踪为“先标归属，再重排中文”，不能只归入泛泛长句。
- **动态 probe：熟词跨语境陌生感**：`alone` 即便知道可译“只有”仍感觉陌生，`precede` 即便知道“在前”也难落成中文。这不是单纯背词问题，而是熟词核心意象没有迁移到当前对象关系：`alone` 是排除其他主体，`precede` 是在时间、顺序、权重或层级上在前。
- **固定表达复发**：0714 的 `give in to` 被按普通 `give` 猜成“投入”，说明“简单词连用先试短语”的预警再次命中，风险不能下调。
- **词形与核心动词**：`demoralization` 被形近联想成“民主”，`vanish` 未落地，导致精神状态和结尾变化两个核心采分点同时丢失。陌生词允许先译粗方向，但不能用无语境支持的熟词替代。
- **回指补全**：0714 的 `which` 回指 `a demoralization of spirit`，原译没有把它补成“这种精神消沉”；这与此前 `in which`、所有格省略的错误共同证明回指风险仍活跃。
- **感知结构**：`feel the oppression vanish` 是 `feel + 宾语 + 动词原形`，重点是感到“压抑消失”。如果只处理 `feel` 和宾语，第二个动词就会从采分点中掉出去。

- **Probe 落地：能力链完整训练**：0714 的 `give in to` 是识别层失败，`blame A on B` 是已识别关系后的语境落点偏差。两层不能混成词表，但也不能各给五句后切断：现改为一个 13 条主训练包，按“熟动词义项过桥六条 → 介词关系落点七条”连续练，目标是把“短语形状—关系—中文落点”接成一条动作。
- **标题、来源与主旨块**：0715 把 `Making It in America`、`a joke from cotton country` 和 `just how much ... has been automated` 拆成字面碎片。它们分别是标题熟语、来源修饰和“程度”主旨，阅读时应先封成三个信息块。
- **双因果论证骨架**：0716 的核心不是一串经济名词，而是“现象主要因为 A，但也因为 B”。没有先锁住 `the reason ... is largely because ... but it is also because ...`，后面所有概念都会互相挤压。
- **论元方向**：0716 的 `replace labor with machines or foreign workers` 被改写成“增长快于替代速度”。这类错误不能只归为词义，必须先固定谁被替代、谁来替代，再决定中文语序。
- **否定比较关系**：0717 的 `nothing would be more important than passing ...` 没有读成“没有什么比……更重要”，而被压成“说得再准不过”。这说明比较级不能只认 `more`；必须把否定主语、比较项和 `than` 后的对象一起封口，才能得到最高级结论。
- **制度语境与从句挂载**：0717 把 `G. I. Bill` 中的 `Bill` 当成人名，又把两个 `that` 压成“确信的几点”。政治语境中 `pass + bill` 应先触发“通过法案”；随后分别确认第一个 `that` 修饰法案、第二个 `that` 引出保障内容。
- **前置伴随块后的倒装**：0718 的 `Along with A came B` 被顺着词序译成“随着 A，他们……”，真正主语 B 没有建立。还原为 `B came along with A` 后，才看得见“永久定居者—临时挣钱者”的两组对照。
- **词义极性校验**：0718 把 `permanent` 译为“短暂”，而后文正有 `no intention to stay`。当同句用正反两组对象构成对照时，应让 `permanent` 与“不打算留下”互相校验；否则一个方向词会带翻整句。

## 批改质量审计

- `0713-day78`：批改覆盖全句主干、两个并列 `that` 从句、`Washington alone` 的代指和排他性、归化规则及联邦法优先关系。Vocab 聚焦 `naturalization / precede / noncontroversial`，Phrases 聚焦制度权力表达，和错误密度匹配。
- `0714-day79`：批改覆盖 `give in to`、`blame A on B`、`which` 回指及 `feel + object + verb`。Vocab 只收精神状态与核心变化词；Phrases 对应真实结构误判，没有扩成泛语法课。
- 本轮今日预警部分命中：固定表达、核心动词和回指均命中真实错误；0715、0716 继续证明预警必须从“找主干”升级为“先把论证骨架和论元方向写出来”。
- `0715-day80`：批改覆盖标题熟语、`relates a joke from cotton country` 的修饰归属、自动化程度和 `keep A away from B` 的方向。词汇、短语与误译位置对应，没有额外堆词。
- `0716-day81`：批改覆盖文章群框架、`make the point that`、双因果结构、`stubbornly high` 和 `replace A with B`。本句错误密度高，但以四块处理，避免把长句拆成零散语法名词。
- `0717-day82`：批改覆盖背景从句、就业语境、否定比较级、`G. I. Bill` 的法案属性、两个 `that` 的挂载及 `have access to` 的机会义。解释从普通比较关系过桥到最高级判断，没有只宣布答案。
- `0718-day83`：批改先还原 `Along with A came B` 的普通语序，再区分两类人及两个 `who` 的归属；词汇和短语集中处理 `permanent`、倒装框架与“安家/无意留下”，与原译错误密度匹配。

## 下周观察

- 句首是长名词短语并夹带 `that/which` 时，强制先圈出全句有限谓语，再处理从句。
- 遇到 `that A and that B`，先验证两个 `that` 是否并列说明同一先行名词。
- 简单动词加介词先按短语试读，尤其是 `give in to`、`blame A on B` 这类会改变论元关系的组合。
- `which/that/it/'s/themselves` 进入中文前必须补出所指对象；不能只凭连接感继续往后读。
- 每个语义块结束前检查核心谓语是否已译出；不认识时先用上下文确定“出现、消失、增强、削弱、优先”等方向。
- 出现 `the reason ... is because ... but also because ...` 时，先写现象与两层原因，再回填名词细节。
- 出现 `replace A with B` 时，先标 A 是被替换者、B 是替代者；中文再转成“用 B 替代 A”。
- 出现 `nothing/no one + more ... than` 时，先完整说成“没有什么/谁比……更……”，再判断是否应收束为最高级。
- 句首出现 `Along with/Into/From ...` 后紧接动词时，不默认前置块里的名词是主语；继续找动词后的名词，并恢复普通语序。
- 同一句出现 `permanent/temporary`、`stay/leave` 等方向词时，用对照关系反查词义正负，避免单词方向翻转后整句跟着错。

## 当前待办

- 使用 `t1/july/0713-day78-structure-drill.md` 训练抽象名词后内容从句中的主句谓语期待位。
- 使用 `t1/july/0713-head-noun-attachment-chain-drill.md` 训练头名词后多个解释块的归属判断与中文重排。
- 使用 `t1/july/0713-day78-meaning-shift-drill.md` 训练熟词跨语境后的核心意象迁移。
- 使用 `t1/july/0714-day79-phrase-chain-drill.md` 训练从短语整体识别到语境落点的完整能力链。
- 后续样本验证：能否在长主语后找到全句谓语，并在双因果句中先保住骨架与替换方向。
