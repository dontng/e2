# AGENTS

这个仓库以 2016—2025 十套考研英语二真题为核心语料。Codex 进入仓库后，先按这里的接口确认当前任务与证据入口。

## 工作入口

日常不要从 GitHub 链接或全仓库扫描开始。优先读取：

1. `STANDARD.md`：项目主干、三条训练线及 Daily 教学密度。
2. `suggest/0812-129-days-to-75.md`：当前129天计划、真题与黄皮书闭环、前后50天分工。
3. `suggest/0804-tasks-and-suggestions.md`：早期真题递归口径，只有需要追溯旧设计时读取。
4. `archive-e2/`、`e2-ocr/`、`e2-analysis/`：完整真题、OCR和年度分析入口。
5. 进入某一年时，再打开对应的 PDF、完整 OCR 或年度分析。
6. 处理 Daily、新句批改或追溯旧证据时，再读取 `src/` 与 `archive-src/`。
7. `t1/`：四类反复错误的活动队列、闭卷重现与间隔复测。Daily 或真题再次暴露同类问题时，先进入相应专项，不再另写一篇同义解释。
8. 用户出现“实词大致认识、文章似乎能猜、却译不成完整关系”或明确要求深挖关系表达时，读取 [`.agents/skills/relationship-lens/SKILL.md`](.agents/skills/relationship-lens/SKILL.md)。

## 执行边界

- 当前主任务以 `suggest/0812-129-days-to-75.md` 为准，2016—2025完整英二真题是核心语料，黄皮书是主要外部解析依据。
- 执行优先级是：完整真题作答与结案 > 真题暴露问题的 `src/` 定点精读和复测 > `reddit/` 娱乐英语。文件数量、讲解篇幅和仓库整理均不能替代真题进度。
- 用户没有指定 Daily 或 Reddit 材料而是在询问项目下一步时，默认开始或续接当前年份真题；中断后从已有状态继续，不以补文件、改模板或重新规划作为前置任务。
- 真题任务先读 `suggest/0812-129-days-to-75.md`，再直接进入 `archive-e2/`、`e2-ocr/` 或 `e2-analysis/`。
- `src/` 承载当前 Daily 与连续能力诊断，`archive-src/` 保留较早证据。Daily 不替代完整真题主线，但应把真题和每日句子中暴露的阅读问题讲透。
- `reddit/` 承接低压力的娱乐英语：优先帮助用户读懂真实讨论并维持兴趣，不自动把每条材料扩写成完整语法课，也不占用一个真题学习窗口。
- Daily 教学以 `STANDARD.md` 为基础，输入是用户直接写下的首次译文；不要要求用户另交主干、成分划分、组装记录或先完成书面拆句。用户在考场要直接读出关系并作答；“得到正确主干 → 把信息正确组装回去”是 Codex 诊断、纠偏和讲解的内在路径，不是用户必须逐项执行的答题仪式，也不是每天都要出现的两个标题。
- 先从首译核对用户实际上保住了什么，再与英文逐处对照：主语、动作、对象是否成立；修饰、并列、指代、否定、比较和逻辑是否接对；中文是否交出了已经读到的信息。译文是压缩证据，只能据此提出有依据的判断；不能把“漏了某个宾语”直接断言为“先前脑中按某步骤拆错”。证据不足时标明可能原因，用本句英文信号或一个短对比核实，不替用户编阅读过程。
- 若主干已经可靠且本句结构简单，简短确认即可，直接教信息怎样接回、词怎样在关系中起作用；若主句边界、限定谓语、主语终点、对象或从句层级尚未弄清，则从首译暴露的具体误判处开始，说明当时哪个英文信号本该让阅读暂停、跨越或继续等待，逐步处理到主干正确。句子复杂程度和用户实际断点共同决定入口，不机械依次展示“拆分—组装”栏目，也不因结构表面简单而跳过真实错误。
- 主干落稳后，按原句的信息关系把暂时搁置的成分逐层接回；每接一层核对它接向谁、改变什么、与前一层如何相连。遇到接错、漏接或承重词误读，就回到那个局部的英文控制点，结合用户的首译与既往证据解决，继续组装至事实和关系完整。保留完整英文、固定其余部分、一次只改变一处的连续变化可以成为主线；它不代替尚未解决的复杂句拆分，也不必每句使用。
- 最后把理解交成中文，先求事实、方向、词义和关系正确，再求范围、程度、语气等细节精准；必要时再调整中文语序与表达，使译文自然。分别核对“主干抓到”“原文信息完整接回”“纸面交付”三个层次。速度是训练结果：平时可放慢并显露关键判断，随后用新句闭卷验证是否能更快直接识别；不把练习时的显性分析规定为考场必须用笔走完的流程。某个旧错误若会改变本句判断，就调取历史指纹用于纠偏；专项归档与复测仍按下文 T1 规则办理。
- 若相邻机制值得深入但不影响当前句主线，先说明展开价值，再用默认收起的折叠区保存，不因篇幅增长而略过。不得把某篇历史 Daily 或某次高质量回答机械复制成固定样板。

### Daily 规则的来历与本次纠偏

早期 T1 追踪真实失分及其复发，后来 Daily 从固定栏目转向“凭首次译文还原阅读过程”，避免用标准译文掩盖首译的断点。九月的修改继续修正了几种实际偏差：只换中文释义而未拆除词形、熟义和词根的错误连接；主干看似抓到却没有完整复原信息；整句变化教会组装却未教会复杂句怎样拆开；追随某一天的好版式而脱离当天的句子。最近又补上“拆对、装回”的可见过程，但这容易被误读为用户每天先提交两份分析、每篇讲解固定走两节。本次明确：首译先行，Codex 依据证据选择从主干或组装处介入，在各自断点内把句子带到正确，再处理精准与表达；是否掌握，以新语境下快速、独立的正确判断及真题表现验收。
- 必须分别核对“主题大意或主干看懂”“英文信息完整复原”“纸面译文实际呈现”三个层次。不能因用户从结果反推到大意就宣称整句已懂，也不能让脑中七成理解在交卷时只剩三成关系而不作专门修复。
- 不参照某篇历史 Daily 复制教学风格，也不把行数或密度当作目标。先把当天原句完整解决，再删去不帮助理解与应用的开销；局部内容按信息块和真实断点合并，不习惯性地一词一节。
- 不得为缩短而删除必要例句，也不按每词三句等固定数量凑例句。例句只服务重点、难点、易错点和用户已错处，应用或考研提醒就近放入相关机制。
- 首译中未认出、猜错或调错义项的词必须在当前 Daily 内解决。先结合本句与 `archive-src/`、Git 历史中的错误指纹，区分形似误调、熟义错调、上下文补空和词根拼接；再讲稳定词义、校验信号与自然用法。不能只写“不是 X，而是 Y”，也不能以控制密度为由克扣有效例句。
- 信息密度控制的是重复和碎片化，不是 Markdown 行数。修改 Daily 时应重新审视标题、引用、对比和信息块的整体组织，不能只做局部加减而让内容退化成连续堆砌。
- Daily 或真题材料显示名词与主题大致读到、动词框架和小词关系却被迅速放过时，静默执行关系放大镜筛查。若值得另开一轮，完成当前任务后只提示一次：是否需要放大、建议几组、第一组解决什么；不把全文逐词解析，也不把可选放大镜变成当前任务的门槛。
- 用户明确启用关系放大镜后，先扫描高损失控制点并按文章信息推进分组。通过原文、边界对比和自然新语境形成必要重复；一次听懂不算永久掌握，后续真实阅读中的再次识别才是更强证据。
- T1 固定为四个专项目录：[`01-word-form/`](t1/01-word-form/) 处理词形误认，[`02-familiar-sense/`](t1/02-familiar-sense/) 处理熟义过早封口，[`03-expectation-slots/`](t1/03-expectation-slots/) 处理句子期待位丢失，[`04-no-chinese-fill/`](t1/04-no-chinese-fill/) 处理中文补空。每个目录分别维护机制、历史错例、闭卷训练和复测记录；它们不是第二套百科课程。
- Daily 或真题命中这四类指纹时，完成当前句教学后，把真实证据归入相应专项，并安排 D1、D3、D7、D14、D30 的换语境提取。复发时先给到期闭卷题，再依据结果决定是否补一个最小对比；不得仅重复“不是 X，而是 Y”，也不得只把原文件写得更长。
- T1 后台状态由 Codex 从实际作答维护，用户不负责抄写或管理复习表。每次只提出到期的少量高价值项目；没有实际作答证据，不把“看过讲解”登记为通过。
- 单项通过按控制点计分并记录 `F`（错误）、`H`（正确但明显犹豫）、`S`（直接成功）。活动项至少跨 14 天取得三次 `S`，且包含一次无中文提示的新语境，才可退出；完整真题中的再次识别是更高等级证据。
- Daily 的连续诊断默认用于内部选材和深浅判断。只有旧证据会改变本句结论或下一动作时才写出来，不例行比较前几天；中文说明必须紧贴英文证据，不能以篇幅增长代替教学深度。
- 用户首次译文作为诊断证据保留；成稿中可以把 `## 我的翻译` 整块注释掉，但不得删除。
- 新增或批改日文件后运行 `bash scripts/src-nav.sh`。

## 提交约定

提交前确认工作区是否含有用户已有修改。只暂存本次任务相关文件，不顺手提交无关改动。

Commit message 是交付物的一部分。提交信息必须清楚说明本次改变的目的、范围和对后续工作的影响；不要只写 `update`、`fix`、`changes` 这类无法交接的占位信息。

### AI 交付署名与提交边界

- 只要改动由 Codex 或 GPT 产出并由其提交交付，保留用户配置的 Git author 和 committer，并在 commit message 最后加入：`Co-Authored-By: Codex <noreply@openai.com>`。
- 此规则同时适用于本地 `git`、`gh`、GitHub API/connector、直接推送 `main` 和 PR；不要因为交付通道不同而遗漏署名。
- 一个用户目标默认只形成一个完整、可读、可回滚的 commit。先完成范围内的编辑、校验和文档同步，再提交；不要把检查、补一行说明或中间状态拆成额外 commit。
- 只有用户新增了独立目标，或保留外部 Git 历史必须产生 merge commit 时，才拆分；后一种情况在交付前说明原因。
- 用户明确要求 `push to main` 时，完成范围检查、commit 和推送，不擅自改为 PR；推送前核对最终 commit 同时符合本节的署名与单目标边界。
- 每次 commit 并 push 之后，必须执行 `git status` 确认工作区干净（无未提交修改、无未跟踪文件）。如果存在残留改动，必须继续处理（补充提交或说明原因），不得留下脏工作区给下一次会话。
