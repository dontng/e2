# AGENTS

这个仓库是英二每日翻译训练库。Codex 进入仓库后，先按这里的接口工作，再按 `STANDARD.md` 的详细格式执行。

## 工作入口

日常不要从 GitHub 链接或全仓库扫描开始。优先读取：

1. `REVIEW.md`：当前审计入口，说明本次要看的周总结和每日文件范围。
2. `STATE.md`：T1 提分状态层，记录当前风险、预警、进度反馈和下一次动作。
3. `reviews/<week>.md`：周总结，记录批改后暴露的问题、批改覆盖缺口和下周观察点。
4. `src/<month>/MMDD-dayN.md`：每日原始证据，只在需要核对具体句子和批改时打开。

## 执行边界

- 有效内容在 `src/`、`t1/`、`reviews/`、`STATE.md`、`REVIEW.md`、`STANDARD.md`。
- `archive/` 和 `archive2/` 是历史归档，日常不参与。
- 每日批改格式以 `STANDARD.md` 为准。
- 批改完成后，把 `## 我的翻译` 整块注释掉，不删除。
- 新增或批改日文件后运行 `bash scripts/src-nav.sh`。

## 审计目标

批改后继续检查四件事：

- 原译暴露了哪些真实能力问题。
- 批改是否覆盖主要误判路线。
- 词汇、短语、Takeaways 是否和错误密度匹配。
- T1 风险线、预警和收益反馈是否随本次样本更新。

如果发现批改质量问题，优先写入当前周总结；只有需要修改每日批改正文时，才回到对应 `src/` 文件。

## T1 口径

`src/` 的每日翻译与批改是 T0，只负责产生真实样本。`STATE.md` 是 T1，只负责把样本滚动成下一次提分的预警、风险权重和进度反馈。

不要把 T1 写成教学文章，也不要维护完整知识白名单。语法、句法、搭配、介词理解和其他考场问题，按真实失分证据进入风险线；量化只看风险是否从 `high` 降到 `medium/low`，以及下一次预警是否命中。

不要默认用户已经按时吸收了全部 T1 内容。重复问题再次出现时，优先引用相关日训练，把旧样本和当天样本合并成一个新的训练挑战；不要重复生成同质解释。T1 的价值是让重复风险产生新的考场动作、对照任务或验证方式。

T1 状态层是 Codex 自己维护的后台体系，不是给用户的主要交付物。面向用户时，优先输出专项训练包：针对一个真实失分风险，给高质量例句或阅读片段，配上理解方向、必要释义和必要生词翻译。训练包文件只保留“目标”和“训练材料”两类前台内容；问题点较窄给 5 条，问题点较大、较难、值得反复咀嚼给 10 条。不要做交互式问答，也不要写低速喂饭式讲解；训练包要像好的代码注释或 commit message 一样清楚、凝练、有推进力。

T1 的自觉性由 Codex 负责，不把系统维护成本转嫁给用户。用户只需要提供每日原译、probe 或自然反馈；Codex 必须自己判断风险线是否过粗、drill 是否打偏、预警是否空炮，并在后台文件中完成修正。面向用户时不要要求用户承担“判断 drill 是否专项”“判断风险线是否该拆”这类后台治理任务。

风险线只做后台总览，不能直接等同于训练入口。生成训练包前，先把风险线拆成可训练的错误指纹：一个错误指纹对应一个考场反射。每个 drill 只训练一个反射，优先给 5 条同构材料；如果候选材料覆盖多个反射，Codex 必须自动拆分或重做，不要产出展示型合集。

## 提交约定

提交前确认工作区是否含有用户已有修改。只暂存本次任务相关文件，不顺手提交无关改动。

Commit message 是交付物的一部分。提交信息必须清楚说明本次改变的目的、范围和对后续工作的影响；不要只写 `update`、`fix`、`changes` 这类无法交接的占位信息。
